---
- name: Unison Deployment Playbook
  hosts: localhost
  connection: local
  gather_facts: yes
  become: yes
  become_method: sudo
  become_user: root
  become_pass: Henith*11
  environment:
    AWS_DEFAULT_REGION: "{{ lookup('env', 'AWS_DEFAULT_REGION') | default('ap-southeast-2', true) }}"
  tasks:
    - include: tasks/app_deploy.yml